# 2026-03-24

- Completed APP-008 in `PerchIOS`: normalized `SeedOrUserPhotoView` so loaded user photos and seeded fallback art share the same full-bleed geometry and clipping, and made the `SpotDetailView` hero explicitly expand to the available width. This targets the reported inconsistency where user-added spot detail pages felt visually larger than seeded/test spot detail pages.
- Validation run from the project root: `plutil -lint PerchIOS/Info.plist`, `plutil -lint PerchIOS.xcodeproj/project.pbxproj`, and `python3 -m json.tool PerchIOS/Resources/SeededSpots.json >/dev/null` all passed.
- `xcodebuild` could not run in this environment because the active developer directory points to Command Line Tools (`/Library/Developer/CommandLineTools`) instead of a full Xcode installation.
